You are a world‑class full‑stack developer building a complete, production‑ready, globally deployable Next.js web app called WordSense AI. The entire application must be flawless, bug‑free, and ready to deploy on Google Cloud Run or Vercel. Use Next.js 14+ with App Router, TypeScript, Tailwind CSS, react‑icons (HiOutlineSearch for search, FiVolume2 for TTS), and the OpenAI ChatGPT API (model gpt‑3.5‑turbo). The core function is an English‑to‑Urdu dictionary: a user searches an English word and instantly gets its Urdu meaning plus one example sentence shown in both English and Urdu. The app must have a search bar with intelligent autocorrect that holds a list of at least 30,000 common English words; on every keystroke (debounced 150ms) it calculates Levenshtein distance using fast‑levenshtein and shows the 5 closest matches in a dropdown; clicking a suggestion fills the input and fetches the meaning. The search bar must be large, rounded, with a search icon inside, and fully keyboard accessible. Fetching the meaning is done via a server‑side API route (app/api/meaning/route.ts) that validates input with zod, checks an in‑memory LRU cache (Map with 24‑hour expiry), then calls OpenAI with a strict system prompt forcing JSON output: { "meaning": "Urdu meaning", "example_en": "English example sentence", "example_ur": "Urdu translation of the example" }. The API must rate‑limit each IP (sliding window, 10 requests per minute) and return proper error responses. The result is displayed in a beautiful card with the English word in bold blue (#2563eb), the Urdu meaning in black, and a volume icon (FiVolume2) next to each that triggers text‑to‑speech via the Web Speech API using lang="en‑US" for English and lang="ur‑PK" for Urdu; if no Urdu voice exists, hide the icon and show a tooltip. While speaking the icon pulses. The card also includes copy and share buttons with toast notifications (react‑hot‑toast). Below the search bar, show recent searches stored in localStorage and a “Random Word” button that picks from a curated list of 100 words and fetches its meaning. For Urdu text, use the Google Font “Noto Nastaliq Urdu” imported via next/font/google and apply it only to Urdu strings; ensure it renders beautifully across all devices. A separate page at /words‑bank displays at least 50 pre‑stored words from data/words.json in a responsive grid (1 col mobile, 2 tablet, 3 desktop). Each card shows the word in blue, meaning in black, and TTS icons. The page includes a client‑side search filter to narrow down words. The entire app must have perfect dark mode support using next‑themes, complete accessibility (keyboard nav, aria‑labels, focus rings, semantic HTML), and excellent SEO (dynamic metadata, Open Graph, Twitter cards, JSON‑LD structured data, robots.ts, sitemap.ts, and a PWA manifest). Performance must be near‑perfect: use server components where possible, lazy‑load non‑critical pieces, cache API responses, and optimize fonts. All browser APIs (speech synthesis, localStorage) must be null‑checked. Include a custom hook useSpeech that manages speaking states and voice availability. Rate‑limit the API route and cache OpenAI responses. The file structure must include everything needed: layout.tsx, home page, words‑bank page, API route, components (SearchBar, WordCard, WordsCard, Navbar, Footer, SkeletonCard, RecentSearches), utilities (cache, wordList, randomWords, rateLimiter, logger), hooks (useSpeech), data/words.json with 50 real English‑Urdu entries, and a comprehensive README.md with local setup and deployment instructions (Google Cloud Run with Dockerfile and Vercel). Provide every file with complete, working code and no placeholders. The final application must be polished, free of TypeScript errors, and ready to be deployed globally without any bugs.